# Purpose

/docker/.dockerignore: Specify which files under PantherExchange to ignore

/docker/docker-compose.yml: Describe the steps to build a series of contains and how they interact

/docker/Dockerfile: Describe the steps to build the central container

/data: docker-compose.yml will use this folder to bind host filesystem to container filesystem for easy access and modifications
